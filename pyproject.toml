[project]
name = "planner-nutricional-whatsapp"
version = "1.0.0"
description = "Sistema de planner nutricional via WhatsApp"
requires-python = ">=3.11"
dependencies = []

[tool.vercel]
entrypoint = "backend.main:app"
